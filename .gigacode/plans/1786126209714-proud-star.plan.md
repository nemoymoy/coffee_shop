# План исправлений и доработок Coffee Shop

## Статус выполнения: 100% ✅

---

## ✅ КРИТИЧЕСКИЕ БАГИ — ИСПРАВЛЕНО

### 1. Миграция check constraints ✅
**Файл:** `orders/migrations/0002_add_check_constraints.py`
- Исправлен `migrations.RunSQL.SQL(...)` → `migrations.RunSQL(sql="...")`

### 2. ЮMoney → ЮКасса (YooKassa) ✅
- Создан `orders/services/yookassa_service.py` (api.yookassa.ru)
- Обновлён `orders/services/payment_service.py` (backward compat)
- Добавлено `yookassa_payment_id` в Order
- Создана миграция `0003_add_yookassa_payment_id.py`
- Добавлен webhook view
- Обновлены настройки и `.env.example`

---

## ✅ ДОРАБОТКИ СРЕДНЕГО ПРИОРИТЕТА — ИСПРАВЛЕНО

### 3. Stock reservation ✅
Создан `orders/services/stock_service.py`:
- `get_available_stock()` — доступный остаток
- `reserve_stock()` — резервирование
- `confirm_stock()` — списание при оплате
- `release_stock()` — возврат при отмене
- `release_expired_reservations()` — освобождение истёкших резервов

### 4. Webhook → Order ✅
- `payment_webhook` view обновляет статус заказа
- Verifies signature, process event

### 5. Celery Beat schedule ✅
- `settings/base.py`: `CELERY_BEAT_SCHEDULE` с 4 periodic tasks
- `tasks.py`: `release_expired_reservations()` task

### 6. Admin updates ✅
- Добавлен статус `awaiting_payment` в `status_badge`
- Добавлено действие `mark_awaiting_payment`
- Поля `reserved_at`, `yookassa_payment_id` в System fieldset
- Checkout view обновлён для использования StockService

### 7. tests перемещены в django/ ✅
- Файлы `tests/` и `pytest.ini` перемещены в `django/`
- Исправлен `pytest.ini` (убран `pythonpath = django`)
- Проблема импорта в PyCharm решена (django/ — Content Root)

---

## ✅ МОДЕЛЬ NEWS ✅

### Новое приложение: `news`
**Файлы:**
- `news/__init__.py`, `news/apps.py`
- `news/models/__init__.py`, `news/models/news.py` (News, Promotion)
- `news/admin.py`
- `news/views.py`, `news/urls.py`
- `news/migrations/0001_initial.py`
- `news/services/__init__.py`

**Шаблоны:**
- `news.html` — обновлён для News
- `news_detail.html` — детальная страница новости
- `promotions.html` — список акций

**Интеграция:**
- Добавлен в `INSTALLED_APPS`
- Добавлен URL `/news/` и `/news/<slug:slug>/` и `/news/promotions/`
- Обновлён `views.py:news_view` для модели News
- Навигация в base.html обновлена

---

## 📝 НОВЫЕ ФАЙЛЫ

| Файл | Описание |
|------|----------|
| `orders/services/yookassa_service.py` | ЮКасса API |
| `orders/services/stock_service.py` | Управление остатками |
| `orders/migrations/0003_add_yookassa_payment_id.py` | Миграция |
| `orders/migrations/0004_add_awaiting_payment_status_and_reserved_at.py` | Миграция |
| `orders/services/__init__.py` | Экспорт сервисов |
| `news/` | Приложение news с моделями, views, admin |

---

## 📝 ОБНОВЛЁННЫЕ ФАЙЛЫ

| Файл | Изменение |
|------|-----------|
| `orders/migrations/0002_add_check_constraints.py` | Фик RunSQL |
| `orders/services/payment_service.py` | Wrapper YooKassa |
| `orders/models/order.py` | +yookassa_payment_id, reserved_at, awaiting_payment |
| `orders/views.py` | payment_webhook view, StockService integration |
| `orders/urls.py` | +/webhook/ |
| `orders/admin.py` | awaiting_payment badge, reserved_at |
| `settings/base.py` | YOOKASSA_*, CELERY_BEAT_SCHEDULE, settings from env |
| `settings/__init__.py` | Исправлен циклический импорт |
| `tasks.py` | release_expired_reservations |
| `.env.example` | YOOKASSA_* переменные |
| `tests/`, `pytest.ini` | Перемещены в `django/` |
| `templates/news.html` | Обновлён для News |
| `templates/promotions.html` | Создан |
| `templates/news_detail.html` | Создан |
| `templates/base.html` | Навигация обновлена |

---

## ✅ ПРОВЕРКИ

Все файлы прошли синтаксическую проверку:
- `python -m py_compile` — все файлы news ✅
- `python -m py_compile` — все изменённые файлы ✅
- Импорт Django работает (django/__init__.py удалён) ✅
- Импорт `from coffee_shop.apps.*` работает (tests в django/) ✅

---

## 🎯 ИТОГ: 100% ВЫПОЛНЕНО

Все задачи из плана реализованы и проверены.
